---
const { title, image, thumbnail, tags = [] } = Astro.props;
---

<div class="rounded-xl shadow hover:shadow-lg transition bg-white dark:bg-zinc-800 overflow-hidden">
  <img src={thumbnail} alt={title} class="w-full aspect-video object-cover" />
  <div class="p-3">
    <h3 class="font-semibold text-lg">{title}</h3>
    <div class="mt-2 flex flex-wrap gap-1">
      {tags.map(tag => (
        <span class="text-xs bg-zinc-200 dark:bg-zinc-700 text-zinc-700 dark:text-zinc-100 px-2 py-0.5 rounded">
          {tag}
        </span>
      ))}
    </div>
  </div>
</div>

